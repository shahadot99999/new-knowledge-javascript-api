//map   The JavaScript Array.map() method allows you to create a new array 
// by applying a function to each element of the original array in a concise, one-line manner. 

const numbers = [1, 2, 3];
const doubled = numbers.map(num => num * 2); // [2, 4, 6]
console.log(doubled);